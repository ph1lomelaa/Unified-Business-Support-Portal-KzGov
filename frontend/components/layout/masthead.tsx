"use client";

// Верхняя служебная полоса («Ресурс группы АО НУХ Байтерек» / «Поддержка
// предпринимателей — 1408») убрана по требованию — шапка начинается сразу с
// брендового блока. Компонент оставлен как no-op, чтобы не трогать разметку
// лейаутов admin/cabinet, которые его импортируют.
export function Masthead() {
  return null;
}
